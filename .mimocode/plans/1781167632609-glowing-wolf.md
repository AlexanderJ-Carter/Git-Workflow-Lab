# Git Workflow Lab 全面完善计划（已执行）

## 执行结果（2026-07-19）

计划中多项问题在仓库里已先期修复；本次按现状重新评估后做了更大胆的一致性重构。

### 已完成

| 项                      | 做法                                                                                               |
| ----------------------- | -------------------------------------------------------------------------------------------------- |
| 导航不统一              | `main.js` 的 `GlobalNavbar.ensure()`：缺导航时自动注入；`test-iframe` 用 `data-no-global-nav` 跳过 |
| 缺 main.js 的用户页     | 批量为 command-builder / search / workspace / diagnostics 等补齐脚本与样式                         |
| 速查表重复              | `cheatsheet.html` → 重定向到更完整的 `command-sheet.html`；全站链接已切换                          |
| command-builder 乱码    | 修复全部 UTF-8 截断字符，去掉旧局部导航                                                            |
| lesson-00 / 00b ID 碰撞 | `get_lesson_id` 将 `lesson-00-terminal-basics` 映射为 `lesson-00b`                                 |
| 缺测课程                | `quiz.html` 补齐 `lesson-00b` 题库；修正 lesson-00 版本命令答案                                    |
| 孤岛页面                | 首页增加 `status.html`、`diagnostics.html`；速查表指向 `command-sheet`                             |
| viewer 路径             | 确认并同步 `site/docs/viewer.html` ← `docs/viewer.html`                                            |
| 测试                    | 新增 `tests/test_site_integrity.py`；扩展 `get_lesson_id` 用例                                     |
| 工具链                  | `pyproject.toml` `requires-python` 调整为 `>=3.9`（匹配 flake8 等依赖）                            |

### 刻意保留

- **sandbox vs playground**：用途不同（多模式沙盒 vs Git 模拟终端），不合并
- **learning-path 重复 CSS / __pycache__**：当前仓库已无这些问题

### 验证

```bash
uv run --with pytest --with pytest-cov --no-project pytest tests/test_build_site.py tests/test_site_integrity.py -v -o addopts=
```

期望：24 passed。

### 辅助脚本（可复用）

- `scripts/fix_site_encoding.py` — 修复 command-builder 乱码
- `scripts/ensure_site_nav.py` — 补齐 main.js / 统一速查表链接
